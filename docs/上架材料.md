# dsh-lark-bridge 生态上架材料（P4）

> 准备给四个目录站/列表的提交内容。执行前先确认仓库已发布（npm `@deepseek-ai/dsh-lark-bridge`）或至少 GitHub 仓库公开可访问。
> 对外提交（PR/表单）由用户确认后执行；本文件只提供材料。

## 一、一句话定位

把 DeepSeek Harness 的全生命周期（思考链、审批、goal/todo、workflow 展开、压缩、后台任务、定时提醒）在飞书/Lark 原生呈现的深度通道插件。

## 二、关键信息

- 仓库：https://github.com/moyu-good/dsh-lark-bridge
- npm：`@deepseek-ai/dsh-lark-bridge`（发布前确认）
- 关键词：`dsh-plugin` `dsh` `deepseek-harness` `feishu` `lark` `messaging` `extension`
- 安装：`npx @deepseek-ai/dsh plugin --profile web add github:moyu-good/dsh-lark-bridge`
- 协议：BSD-3-Clause
- 测试：260 tests / 41 contract checks / 契约漂移 CI（对 deepseek-harness master）
- README 对比表：README.md「vs. other Feishu/Lark bridges」/ README.zh.md「与其他飞书/Lark 桥对比」

## 三、差异化卖点（提交描述用）

1. 全生命周期可视：原生 CoT 思考过程 + goal/todo 实时卡片 + workflow run/member/phase/log 文本流 + 压缩透明化
2. 断点续跑：会话持久化 + `autoResumeGoals` 重启后自动续跑活跃目标
3. 决策留痕：审批卡片回写决策人与结果 + `/audit` 会话审计
4. 会话全文检索：`/sessions <关键词>` 检索本聊天历史
5. 后台任务与子代理结算通知
6. 免公网回调（WebSocket 长连接）+ 扫码注册 + 编译产物直装

## 四、各入口提交模板

### 1. dshfind.com
- 类型：插件收录表单
- 提交内容：仓库 URL、npm 包名、关键词、README URL、上面的一句话定位
- 备注：dshfind 分类里飞书桥已有 70+，提交时把「dsh-plugin topic + 深度原生呈现」写清楚，避免被归为普通消息转发

### 2. dshbase.com
- 类型：仓库收录表单/PR
- 提交内容：同上 + 维护状态（近期提交）、CI 徽章、许可证
- 备注：dshbase 偏目录聚合，给 GitHub 仓库地址即可，README 需有清晰安装/配置段（已有）

### 3. dsh-plugin.org
- 类型：列表收录
- 提交内容：名称、仓库、一句话描述、标签（feishu/lark/channel）
- 备注：如果站点要求 YAML/front matter，按站内模板填

### 4. awesome-dsh-plugin（GitHub，★956）
- 类型：PR 追加一行
- 仓库：https://github.com/<owner>/awesome-dsh-plugin（按实际）
- PR 内容示例：

```markdown
- [dsh-lark-bridge](https://github.com/moyu-good/dsh-lark-bridge) - Feishu/Lark IM channel with native thinking process, approval cards, live goal/todo cards, workflow fan-out, session search and background job notifications. `dsh-plugin`
```

- PR 说明：附测试数、CI 链接、对比表链接，说明为何值得收录（深度 Harness 原生呈现而非消息转发）

## 五、执行清单（等用户点头）

- [ ] npm publish（先确认 package.json version、`files` 含 lib/src/README/LICENSE/cordis.patch.yml）
- [ ] dshfind.com 提交
- [ ] dshbase.com 提交
- [ ] dsh-plugin.org 提交
- [ ] awesome-dsh-plugin PR
- [ ] 发布后把「已上架」状态回写 P007/P008
