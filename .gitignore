node_modules/
*.tsbuildinfo
*.log
.env
.DS_Store

# 本地运维脚本(含本机路径)
run-*.sh
# node 编译缓存
node-compile-cache/

# 本地备份误入版本库防线（run-*.sh 不匹配带时间戳的 .bak 变体）
run-*.sh.bak-*

# journey 私有产物（含本机路径/实测报告，不入公仓）
journey/report-*.json
journey/基线审计*.md
journey/s1-probe.sh

# 部署私有词表：本仓检查脚本的补充模式（内部项目名/私人称呼/私有主机名）
# 必须留在本地——把这些词写进仓库，等于用泄漏清单去泄漏。
# 模板见 .leak-patterns.example
.leak-patterns
